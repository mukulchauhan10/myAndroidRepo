package com.example.roomdbapplication.database;

/*import android.content.Context;

import androidx.room.Room;*/

public class DataBaseClient2 {
   /* private Context mContext;
    private TaskDatabase taskDatabase;
    private static DataBaseClient mInstance;

    public DataBaseClient2(Context mContext) {
        this.mContext = mContext;
        taskDatabase = Room.databaseBuilder(mContext, TaskDatabase.class, "MyTask").build();
    }

    public static synchronized DataBaseClient2 getInstance(Context mContext) {
        if (mInstance == null)
            mInstance = new DataBaseClient(mContext);
        return mInstance;
    }

    public TaskDatabase getTaskDatabase() {
        return taskDatabase;
    }*/
}
